services:
  - type: web
    name: medical-assistant-api
    env: python
    buildCommand: pip install -r server/requirements.txt
    startCommand: cd server && uvicorn main:app --host 0.0.0.0 --port 8000
